<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
    <style media="screen">
      form{
        display: inline;
      }
      h3{
        color: red;
      }
    </style>
  </head>
  <body>
    <h1>Welcome to QuotingDojo</h1>
    <form class="" action="/add" method="post">
      <label for="">Your Name:</label>
      <input type="text" name="name" value="">
      <% if(typeof(errors) != 'undefined' ) { %>
          <% for (var x in errors) { %>
            <% if (x=='name') { %>
              <h3><%= errors[x].message %></h3>
            <% } %>
          <% } %>
      <% } %>
    </br></br>
      <label for="">Your quote:</label>
      <textarea name="quote" rows="8" cols="50"></textarea>
      <% if(typeof(errors) != 'undefined' ) { %>
          <% for (var x in errors) { %>
            <% if (x=='quote') { %>
              <h3><%= errors[x].message %></h3>
            <% } %>
          <% } %>
      <% } %></br></br>
      <input type="submit" name="" value="Add my quote!">
    </form> &nbsp&nbsp&nbsp&nbsp&nbsp &nbsp&nbsp&nbsp&nbsp&nbsp
    <form class="" action="/quotes" method="get">
      <input type="submit" name="" value="Skip to quotes">
    </form>
  </body>
</html>
